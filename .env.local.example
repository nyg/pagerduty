# PagerDuty API token (server-side, not exposed to browser)
PAGERDUTY_API_TOKEN=

# Optional: pre-configure settings (client-side env vars)
NEXT_PUBLIC_PAGERDUTY_TEAM_ID=
NEXT_PUBLIC_NGROK_URL=
NEXT_PUBLIC_POLL_INTERVAL=30
